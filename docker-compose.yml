services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: doski_dev
    ports:
      - "5432:5432"
    volumes:
      - doski_pg:/var/lib/postgresql/data

volumes:
  doski_pg:
